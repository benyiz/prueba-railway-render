services:
  db_test:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pokeapi_test
    tmpfs:
      - /var/lib/postgresql/data